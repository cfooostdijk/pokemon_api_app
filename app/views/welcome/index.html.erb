<%= render(MainBoxComponent.new("Welcome to the Pokémon Api App")) %>

<div class='flex flex-col mt-5 items-center'>
  <p>Please get started by searching for a Pokemon below:</p>
  <%= render(SearchComponent.new("#{@pokemon}")) %>
</div>
